import order from "./order";
import category from "./category";
import product from "./product";
import configuration from "./configuration";

export default { order, category, product, configuration };
